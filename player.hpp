#include <iostream>
#include <stdlib.h>
#include <string>
#include <vector>
#include <set>
#include <time.h>

extern int hp;
extern int mhp;
extern int matk;
extern int mdef;
extern int atk;
extern int def;
extern int name;
extern long long drop;
extern const char* inv[6];

/// @brief 
/// @param a 
/// @param b 
/// @param c 
void playerc(int a, int b, int c){
    a = hp;
    b = atk;
    c = def;
}
